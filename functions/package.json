{
  "name": "hejapp-functions",
  "description": "Cloud Functions for HejApp push notifications",
  "version": "1.0.0",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "echo 'JS project'",
    "deploy": "firebase deploy --only functions:onMessageCreated",
    "lint": "echo 'skipping lint'"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.6.0"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0",
    "picomatch": "^4.0.3"
  }
}
